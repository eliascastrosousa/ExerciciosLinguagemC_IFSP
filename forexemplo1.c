
#include <stdio.h>
#include <locale.h>

main() {

setlocale(LC_ALL, "Portuguese");

int inferior, sup, par, numero, contador, soma;

soma =0;

do {
    printf("Limite inf: ");
    scanf("%d",&inf);

    printf("Limite sup: ");
    scanf("%d",&sup);

}while (inferior < superior);
inf++;
sup--;
for(contador=1; contador <= sup;  )

    if (contador %2 = 0){
      printf(contador);
    soma = soma+contador;
    }



}
